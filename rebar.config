{deps, [grisp]}.

{erl_opts, [debug_info]}.

{grisp, [
    {otp, [
        {version, "22.0"}
    ]},
    {deploy, [ % ne jamais déployer -> effacer ça?
        {destination, "/tmp/sd_card"}
    ]}
]}.

{shell, [{apps, []}]}.

{relx, [
    {release, {hera, "0.1.0"}, [hera]}
]}.
